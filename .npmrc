# Polaris 13 declares a peer dependency on React 18 and has no React 19 release
# yet, so a plain install fails to resolve. Verified working against React 19:
# Polaris does not use findDOMNode, defaultProps on function components, or the
# other APIs React 19 removed. Required for CI/Vercel installs to succeed.
legacy-peer-deps=true
